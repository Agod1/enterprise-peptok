/**
 * Sample Data Initializer - DISABLED
 *
 * This utility has been disabled as part of the complete dummy data cleanup.
 * All sample/mock data has been removed from the system.
 */

export function initializeSampleData() {
  console.log(
    "🚫 Sample data initialization is disabled - system uses real data only",
  );
  // This function is intentionally empty - all dummy data has been removed
}
